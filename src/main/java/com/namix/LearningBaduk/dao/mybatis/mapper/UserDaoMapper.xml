<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.namix.LearningBaduk.dao.UserDao">

	<select id="login" resultType="com.namix.LearningBaduk.entity.User">
		select u.*, r.userRole from user as u inner join role as r on u.userId = r.userId where u.userId = #{id} and u.userPassword = #{password}
	</select>

</mapper>